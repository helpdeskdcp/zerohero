# Data Collection Status (Phase 12)

Generated by `app.reverse_engineering.status_report.build_status_report()` against
real DBs at 2026-09-21. Full numbers: `DATA_COLLECTION_STATUS.json` (same directory).
Re-run the function any time for a fresh snapshot — this file is a point-in-time
summary, not a live view.

## Summary

- **events_captured**: 0 — `research_events.db` is a new, empty store; the 5 known
  signals from `PHASE0_PHASE1_REPORT.md` exist only as a markdown table, not yet
  loaded via `schema.insert_event`.
- **model_readiness**: `NOT_READY / INSUFFICIENT_SAMPLE` (0 of 100 required events).
- **confirmation_engine_readiness**: `BUILT_SHADOW_ONLY` — implemented, unit-tested,
  not wired into any live/paper path.
- **orderflow_availability**: `True` for NIFTY (resting order-book only, see note in
  the JSON — not trade-level aggressor flow).
- **data_coverage**: real per-symbol/kind date ranges from `market_history.db` —
  NIFTY/CRUDEOIL/NATURALGAS from 2026-09-01, most NSE/BSE indices from 2026-09-03/04,
  all through 2026-09-18. See JSON `data_coverage.by_symbol_kind`.
- **option_chain_density**: per-session snapshots-per-strike across all captured
  symbols/dates — confirms the same sparsity pattern found for the single SENSEX
  76500 PE case in Phase 0/1 is systemic across symbols and dates (see JSON
  `option_chain_density.per_session`; most sessions sit at 1-3 snapshots/strike).

## Next step to change this

Loading the known 5 signals into `research_events.db` via `schema.insert_event` would
make `events_captured` real; it would not change `model_readiness` (still ~20x below
the 100-event floor) or the underlying option-chain sparsity.
